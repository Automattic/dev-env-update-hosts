name: Release

on:
  push:
    tags:
      - '**'

permissions:
  contents: read

jobs:
  prepare_release:
    name: Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
      attestations: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push container image
        uses: docker/build-push-action@v7
        id: push
        with:
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
          outputs: type=local,dest=out

      - name: Compress artifacts and generate checksums
        run: for i in dev-env-update-hosts-*; do gzip -9 "${i}"; sha256sum "${i}.gz" | awk '{print $1}' > "${i}.gz.sum"; done
        working-directory: out

      - name: Attest build provenance
        id: attest
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: |
            out/dev-env-update-hosts-*

      - name: Generate provenance file
        run: |
          jq .dsseEnvelope "${{ steps.attest.outputs.bundle-path }}" > out/dev-env-update-hosts.intoto.jsonl

      - name: Create a release
        uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true
          files: |
            out/*.gz
            out/*.gz.sum
            out/dev-env-update-hosts.intoto.jsonl
